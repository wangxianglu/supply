spring:
  profiles:
    active: dev
  mvc:
    throw-exception-if-no-handler-found: true
  resources:
    add-mappings: true
banner:
  location: banner-cnm.txt

logging:
  level:
    com:
      dianmei: debug